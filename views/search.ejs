<h1 class="pb-4">Search Results</h1>

<div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
  <% if(event != '') { %>
    <% event.forEach(function(event, index){ %>
    <a href="/events/<%= event._id %>" class="col text-center category__link">
      <div class="category__img category__img--large shadow">
        <img src="/eventimages/<%= event.image %>" alt="<%= event.eventname %>" loading="lazy">
      </div>
      <div class="pt-1"><%= event.eventname %></div>
    </a>
    <% }) %>
  <% } %>
</div>